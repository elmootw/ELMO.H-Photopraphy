{
  "name": "elmo-photography",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node build-manifest.js && vite",
    "build": "node build-manifest.js && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.32",
    "autoprefixer": "^10.4.16",
    "vite": "^5.0.0",
    "gh-pages": "^6.1.0"
  }
}
